version: "3"

services:
  api:
    build:
      dockerfile: Dockerfile
      context: ../api

    ports:
      - "8000:8000"

  website:
    build:
      dockerfile: Dockerfile
      context: ../api

    ports:
      - "8080:3000"
